from damri.split import one2one


val_size = 2
n_add_ids = 5
pretrained = False

split = one2one(dataset.df, val_size=val_size, n_add_ids=n_add_ids,
                train_on_add_only=pretrained, seed=seed)
